import React from 'react';
import { Feather } from '@expo/vector-icons';

const ShoppingListDeleteIcon = ({ color }: { color: string }) => {
  return null
  // return <Feather name="trash-2" size={24} color="white" />;
};

export default ShoppingListDeleteIcon;
