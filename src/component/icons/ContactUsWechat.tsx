import React from 'react';
import { AntDesign } from '@expo/vector-icons';

const ContactUsWechat = ({ color }: { color: string }) => {
  return <AntDesign name="wechat" size={24} color={color} />;
};

export default ContactUsWechat;
